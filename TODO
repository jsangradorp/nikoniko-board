- Convert the procedural subview rendering into collections
- Fetch all reportedFeelings along the board in a single call
- Create a slightly more stylish boards page
- Make it work on mobile
- Check that from and until parameters work well.
- Hint to use HTTPS or refuse to use HTTP
